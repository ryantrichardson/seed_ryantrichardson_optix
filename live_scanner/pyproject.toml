[project]
name = "live-scanner"
version = "0.1.0"
description = "Live ghost-wick detector + dark-pool block tape via Massive WebSocket"
requires-python = ">=3.10"
dependencies = [
    "massive-api-client>=1.14.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]
